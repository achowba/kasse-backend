{
  "openapi": "3.0.0",
  "paths": {
    "/api/v1/auth/signup": {
      "post": {
        "description": "Creates an account and returns a token pair, so a client does not have to log in immediately afterwards.\n\nThe password must be at least 12 characters. There are no composition rules: length is what resists an offline attack, while symbol requirements mostly produce predictable substitutions. It is hashed with Argon2id and never stored, logged, or returned.\n\nThis is the only route that admits an address is already registered, because a registration form cannot be usable otherwise. Login never distinguishes an unknown address from a wrong password.",
        "operationId": "AuthController_signup_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CredentialsDTO"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The account was created and a session started.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthResponseDTO"
                }
              }
            }
          },
          "409": {
            "description": "The email address is already registered.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "429": {
            "description": "Too many attempts. Wait and retry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "summary": "Register an account",
        "tags": [
          "Auth"
        ]
      }
    },
    "/api/v1/auth/login": {
      "post": {
        "description": "Exchanges an email and password for a token pair.\n\nAn unknown address and a wrong password return the same 401 with the same message, and take the same amount of work, so this endpoint cannot be used to discover which addresses are registered.\n\nSend the access token as `Authorization: Bearer <token>`. It is short lived; use the refresh token to get a new one rather than asking for the password again.",
        "operationId": "AuthController_login_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CredentialsDTO"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The session was started.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The email or password is wrong.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "429": {
            "description": "Too many attempts. Wait and retry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "summary": "Start a session",
        "tags": [
          "Auth"
        ]
      }
    },
    "/api/v1/auth/refresh": {
      "post": {
        "description": "Exchanges a refresh token for a new access and refresh token pair.\n\nRefresh tokens are single use. The presented token is revoked as part of the exchange, so the new one must replace it on the client. Both operations happen in one transaction, so a failure cannot leave a session without a usable refresh token.\n\nPresenting a token that was already exchanged means the token leaked, and every session descended from that login is revoked. Both the legitimate user and whoever stole the token are signed out, which is deliberate: a noisy failure is better than a quiet ongoing compromise.\n\nNo access token is required, since the refresh token is itself the credential.",
        "operationId": "AuthController_refresh_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RefreshTokenDTO"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "A new token pair.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AuthResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The token is unknown, already used, expired, or its account no longer exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "429": {
            "description": "Too many attempts. Wait and retry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "summary": "Renew a session",
        "tags": [
          "Auth"
        ]
      }
    },
    "/api/v1/auth/logout": {
      "post": {
        "description": "Revokes the refresh token supplied in the body, ending that session.\n\nIdempotent: a token that is unknown, already revoked, or belongs to another account produces the same 204, because in every case the caller's intent is satisfied and the session is not active. It also means this cannot be used to probe for another account's tokens.\n\nThe access token is not revoked and remains valid until it expires, which is minutes. That is the tradeoff of stateless verification.",
        "operationId": "AuthController_logout_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RefreshTokenDTO"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "The session is no longer active."
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "429": {
            "description": "Too many attempts. Wait and retry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "End the current session",
        "tags": [
          "Auth"
        ]
      }
    },
    "/api/v1/auth/sessions": {
      "get": {
        "description": "Returns every live, unexpired session on the account, newest first.\n\nSessions are identified by when they started and when they were last used, not by a device or browser name. Producing such a name means storing the user agent, and this service does not persist identifiers it does not need.",
        "operationId": "AuthController_listSessions_v1",
        "parameters": [],
        "responses": {
          "200": {
            "description": "The active sessions.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/SessionResponseDTO"
                  }
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "429": {
            "description": "Too many attempts. Wait and retry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List active sessions",
        "tags": [
          "Auth"
        ]
      },
      "delete": {
        "description": "Revokes every refresh token on the account, including the one belonging to the caller.\n\nEnding the current session too is deliberate. The reason to press \"sign out everywhere\" is usually not knowing which device is compromised, and sparing the device in your hand would defeat that.\n\nExisting access tokens stay valid until they expire, which is minutes.",
        "operationId": "AuthController_revokeAllSessions_v1",
        "parameters": [],
        "responses": {
          "204": {
            "description": "Every session was ended."
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "429": {
            "description": "Too many attempts. Wait and retry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "End every session",
        "tags": [
          "Auth"
        ]
      }
    },
    "/api/v1/auth/sessions/{sessionId}": {
      "delete": {
        "description": "Revokes a single session by its identifier, taken from the session list.\n\nA session belonging to another account answers 404 rather than 403, so this cannot be used to confirm that another account's session exists.",
        "operationId": "AuthController_revokeSession_v1",
        "parameters": [
          {
            "name": "sessionId",
            "required": true,
            "in": "path",
            "description": "Identifier from the session list.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0a1",
              "allOf": [
                {
                  "$ref": "#/components/schemas/ObjectId"
                }
              ]
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The session was ended."
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "No such live session on this account.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "429": {
            "description": "Too many attempts. Wait and retry.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "End one session",
        "tags": [
          "Auth"
        ]
      }
    },
    "/api/v1/me": {
      "get": {
        "description": "Returns the account the access token belongs to, including the currency and fiscal year start that reports are computed against. The password hash is never returned.",
        "operationId": "UsersController_getMe_v1",
        "parameters": [],
        "responses": {
          "200": {
            "description": "The signed in account.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The account no longer exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Read the signed in account",
        "tags": [
          "Account"
        ]
      },
      "patch": {
        "description": "Changes the currency or the month the fiscal year starts in. Both are optional; anything omitted is left alone.\n\nChanging the currency relabels amounts rather than converting them. Amounts are stored as minor units with no currency of their own, so existing plans and expenses keep their numeric value.\n\nThe email address is not changeable here, because it is the login identity and moving it needs a verification flow.",
        "operationId": "UsersController_updateMe_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateMeDTO"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The updated account.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UserResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The account no longer exists.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Update account settings",
        "tags": [
          "Account"
        ]
      }
    },
    "/api/v1/audit-log": {
      "get": {
        "description": "Returns changes to this account's financial data, newest first.\n\nEvery change to a plan, an expense, a category, or a period lock is recorded with the state before and after, so the trail answers what changed rather than only what the record says now. Because deletes are soft and audited, a deleted record's last known state is still readable here.\n\nEach entry carries the `requestId` of the request that made the change, which matches the `x-request-id` response header and the service logs for the same request.\n\nFilter by `entity` and `entityId` to read the history of one record.\n\nThe trail is append only. No route in this API can edit or remove an entry.",
        "operationId": "AuditLogController_list_v1",
        "parameters": [
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "How many records to return.",
            "schema": {
              "minimum": 1,
              "maximum": 200,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "offset",
            "required": false,
            "in": "query",
            "description": "How many records to skip.",
            "schema": {
              "minimum": 0,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "entity",
            "required": false,
            "in": "query",
            "description": "Restrict to one kind of record.",
            "schema": {
              "type": "string",
              "enum": [
                "CATEGORY",
                "PLAN",
                "EXPENSE",
                "PERIOD_LOCK",
                "IMPORT_BATCH",
                "REPORT"
              ]
            }
          },
          {
            "name": "action",
            "required": false,
            "in": "query",
            "description": "Restrict to one kind of change.",
            "schema": {
              "type": "string",
              "enum": [
                "CATEGORY_CREATED",
                "CATEGORY_UPDATED",
                "CATEGORY_DELETED",
                "PLAN_CREATED",
                "PLAN_UPDATED",
                "PLAN_DELETED",
                "EXPENSE_CREATED",
                "EXPENSE_UPDATED",
                "EXPENSE_DELETED",
                "PERIOD_LOCKED",
                "PERIOD_UNLOCKED",
                "IMPORT_COMPLETED",
                "NL_QUERY_RUN"
              ]
            }
          },
          {
            "name": "entityId",
            "required": false,
            "in": "query",
            "description": "Restrict to the history of a single record.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0a2",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of the trail, newest first.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "items",
                    "pagination"
                  ],
                  "properties": {
                    "items": {
                      "type": "array",
                      "items": {
                        "$ref": "#/components/schemas/AuditEntryResponseDTO"
                      }
                    },
                    "pagination": {
                      "$ref": "#/components/schemas/PaginationDTO"
                    }
                  }
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Read the audit trail",
        "tags": [
          "Audit log"
        ]
      }
    },
    "/api/v1/categories": {
      "get": {
        "description": "Returns the caller's own categories together with the shared catalogue, sorted by name.\n\nEach entry reports whether it is `shared`. A shared category can be selected by any account but cannot be renamed, archived, or deleted, so a client should not offer those actions for it.\n\nArchived categories are excluded by default, because a picker wants only what can be chosen now. Pass `includeArchived=true` to see them; they still resolve in existing plans, expenses, and reports.",
        "operationId": "CategoriesController_list_v1",
        "parameters": [
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "How many records to return.",
            "schema": {
              "minimum": 1,
              "maximum": 200,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "offset",
            "required": false,
            "in": "query",
            "description": "How many records to skip.",
            "schema": {
              "minimum": 0,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "includeArchived",
            "required": false,
            "in": "query",
            "description": "Include categories that have been archived. Off by default, since a picker wants only selectable ones.",
            "schema": {
              "default": false,
              "example": false,
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of selectable categories.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "items",
                    "pagination"
                  ],
                  "properties": {
                    "items": {
                      "type": "array",
                      "items": {
                        "$ref": "#/components/schemas/CategoryResponseDTO"
                      }
                    },
                    "pagination": {
                      "$ref": "#/components/schemas/PaginationDTO"
                    }
                  }
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List selectable categories",
        "tags": [
          "Categories"
        ]
      },
      "post": {
        "description": "Creates a category owned by the caller, alongside the shared catalogue.\n\nNames are compared case and spacing insensitively, so \"Cloud Hosting\" and \"cloud  hosting\" collide and the second is rejected. Without that, a picker ends up showing what looks like the same category twice and a report splits the same spend across both.\n\nThe comparison covers **everything the caller can see**, their own categories and the shared catalogue alike, so a personal category cannot take the name of a shared one. The database constraint alone does not prevent that, since it is keyed on the owner and the two rows have different owners.",
        "operationId": "CategoriesController_create_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateCategoryDTO"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The category was created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CategoryResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "409": {
            "description": "The name is already used by one of the caller’s categories or by the shared catalogue.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Create a category",
        "tags": [
          "Categories"
        ]
      }
    },
    "/api/v1/categories/{categoryId}": {
      "patch": {
        "description": "Changes the name, the archived state, or both. Anything omitted is left alone.\n\nOnly the caller's own categories can be changed. A shared catalogue entry, which the list marks with `shared: true`, answers **403** and says so. It is listed to the caller, so answering \"not found\" would contradict the list they just read and leave a client unable to tell a read only category from a missing one.\n\nAn id belonging to another account answers **404**, the same as an id that never existed, because confirming it exists would reveal that another account holds it.\n\nArchiving hides a category from pickers without affecting anything historic: existing plans and expenses keep resolving it and reports are unchanged. It is almost always what someone wants instead of deleting.",
        "operationId": "CategoriesController_update_v1",
        "parameters": [
          {
            "name": "categoryId",
            "required": true,
            "in": "path",
            "description": "Identifier from the category list.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0a1",
              "allOf": [
                {
                  "$ref": "#/components/schemas/ObjectId"
                }
              ]
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateCategoryDTO"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The updated category.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CategoryResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "403": {
            "description": "The id is a shared catalogue entry, which cannot be changed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "No category with that id belongs to the caller.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "409": {
            "description": "The new name is already used by one of the caller’s categories or by the shared catalogue.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Rename or archive a category",
        "tags": [
          "Categories"
        ]
      },
      "delete": {
        "description": "Removes a category the caller owns from every list.\n\nThe delete is soft. The record survives so that plans and expenses in a locked period keep resolving the name they referenced, and so a mistaken delete is recoverable without a database restore. The change is recorded in the audit log with the category's last state.\n\nConsider archiving instead. Deleting a category that already has spend against it makes those records harder to read, while archiving hides it from pickers and leaves history intact.\n\nA shared catalogue entry, marked `shared: true` in the list, answers **403**: the seeded catalogue belongs to every account, so no one account may remove an entry from under the others. An id belonging to another account answers **404**.",
        "operationId": "CategoriesController_remove_v1",
        "parameters": [
          {
            "name": "categoryId",
            "required": true,
            "in": "path",
            "description": "Identifier from the category list.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0a1",
              "allOf": [
                {
                  "$ref": "#/components/schemas/ObjectId"
                }
              ]
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The category was deleted."
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "403": {
            "description": "The id is a shared catalogue entry, which cannot be deleted.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "No category with that id belongs to the caller.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Delete a category",
        "tags": [
          "Categories"
        ]
      }
    },
    "/api/v1/period-locks": {
      "get": {
        "description": "Returns the months that are closed, oldest first, optionally within a range.\n\nNot paginated. An account has at most a handful of locked months per year, and a client needs all of them at once to mark the right rows read only in a report.\n\nThe absence of a month from this list is what \"open\" means. There is no unlocked record.",
        "operationId": "PeriodLocksController_list_v1",
        "parameters": [
          {
            "name": "from",
            "required": false,
            "in": "query",
            "description": "First month of the range, inclusive.",
            "schema": {
              "example": "2026-01",
              "type": "string"
            }
          },
          {
            "name": "to",
            "required": false,
            "in": "query",
            "description": "Last month of the range, inclusive.",
            "schema": {
              "example": "2026-12",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The closed periods.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/PeriodLockResponseDTO"
                  }
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List closed periods",
        "tags": [
          "Period locks"
        ]
      },
      "post": {
        "description": "Closes months so their plans and expenses become read only.\n\nSupply either `months` or `quarter`. A quarter expands to its three calendar months, so the stored shape is always one record per month and a single month of a closed quarter can be reopened without a special case.\n\nClosing an already closed month is harmless: it is skipped and its original time is preserved, so locking a quarter that overlaps a month closed earlier does not rewrite when that happened. The response lists only the months this call actually closed.\n\nAfterwards, any attempt to create, change, or delete a plan or an expense in those months is rejected with `423` and the code `PERIOD_LOCKED`, including rows inside a CSV import. Moving a record out of a closed month is rejected too, because that changes the closed month's totals.",
        "operationId": "PeriodLocksController_lock_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreatePeriodLockDTO"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The months this call closed. Already closed months are omitted.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Neither months nor a quarter was supplied, or a month was malformed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Close periods",
        "tags": [
          "Period locks"
        ]
      }
    },
    "/api/v1/period-locks/{month}": {
      "delete": {
        "description": "Reopens a single month, making its plans and expenses editable again.\n\nReopening is recorded in the audit log, with the month and the request that did it, so a period that was closed and then reopened leaves a trail rather than looking as though it was never closed.\n\nA month that is not closed answers 404.",
        "operationId": "PeriodLocksController_unlock_v1",
        "parameters": [
          {
            "name": "month",
            "required": true,
            "in": "path",
            "description": "The month to reopen, as YYYY-MM.",
            "schema": {
              "example": "2026-01",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The period is open."
          },
          "400": {
            "description": "The month is malformed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "That month is not closed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Reopen a period",
        "tags": [
          "Period locks"
        ]
      }
    },
    "/api/v1/plans": {
      "get": {
        "description": "Returns monthly targets, newest month first, optionally filtered by month range and category.\n\nEach row carries the category id rather than its name. Resolving names here would mean a lookup per row, and a client listing targets already holds the category list it built its picker from. The report endpoint does include names, because there the join happens once inside the aggregation.",
        "operationId": "PlansController_list_v1",
        "parameters": [
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "How many records to return.",
            "schema": {
              "minimum": 1,
              "maximum": 200,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "offset",
            "required": false,
            "in": "query",
            "description": "How many records to skip.",
            "schema": {
              "minimum": 0,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "from",
            "required": false,
            "in": "query",
            "description": "First month of the range, inclusive.",
            "schema": {
              "example": "2026-01",
              "type": "string"
            }
          },
          {
            "name": "to",
            "required": false,
            "in": "query",
            "description": "Last month of the range, inclusive.",
            "schema": {
              "example": "2026-12",
              "type": "string"
            }
          },
          {
            "name": "categoryId",
            "required": false,
            "in": "query",
            "description": "Restrict to one category.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0a1",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of targets.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "items",
                    "pagination"
                  ],
                  "properties": {
                    "items": {
                      "type": "array",
                      "items": {
                        "$ref": "#/components/schemas/PlanResponseDTO"
                      }
                    },
                    "pagination": {
                      "$ref": "#/components/schemas/PaginationDTO"
                    }
                  }
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List targets",
        "tags": [
          "Plans"
        ]
      },
      "put": {
        "description": "Sets the target for one category and one month, creating it or replacing it.\n\nAddressed by category and month rather than by id, because that is how a target is thought about: a cell in a grid. Sending the same request twice updates the cell rather than creating a second target, so a client never has to know whether one already existed. A unique index enforces that, so two requests racing on the same cell also produce one record.\n\nA target of `0` is meaningful and is not the same as having no target. Zero means nothing was planned, so spend against it is reported as unplanned with a `null` variance percentage rather than an infinite one. No target at all means the cell is absent from the plan side of the report entirely.\n\nRejected with `423` when the month is closed, and with `404` when the category is not one the caller can use.",
        "operationId": "PlansController_upsert_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpsertPlanDTO"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The stored target.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlanResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The category is not one the caller can use.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "423": {
            "description": "The month is closed. The response carries the month in `details`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Set a target",
        "tags": [
          "Plans"
        ]
      }
    },
    "/api/v1/plans/{planId}": {
      "patch": {
        "description": "Changes the amount of an existing target.\n\nOnly the amount. Moving a target to a different category or month is not an edit but a different cell: it would vacate one cell and overwrite another, and either end could be in a closed period. Set the new cell and delete the old one, so each goes through its own lock check.\n\nRejected with `423` when the target's month is closed.",
        "operationId": "PlansController_update_v1",
        "parameters": [
          {
            "name": "planId",
            "required": true,
            "in": "path",
            "description": "Identifier from the plan list.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0b1",
              "allOf": [
                {
                  "$ref": "#/components/schemas/ObjectId"
                }
              ]
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdatePlanDTO"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The updated target.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlanResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The caller has no target with that id.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "423": {
            "description": "The month is closed. The response carries the month in `details`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Change a target amount",
        "tags": [
          "Plans"
        ]
      },
      "delete": {
        "description": "Removes a target, so the category and month no longer appear on the plan side of a report.\n\nThe delete is soft. The audit trail keeps what the target was, so a report run before the deletion can still be explained, and the cell can be planned again afterwards.\n\nRejected with `423` when the target's month is closed.",
        "operationId": "PlansController_remove_v1",
        "parameters": [
          {
            "name": "planId",
            "required": true,
            "in": "path",
            "description": "Identifier from the plan list.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0b1",
              "allOf": [
                {
                  "$ref": "#/components/schemas/ObjectId"
                }
              ]
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The target was removed."
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The caller has no target with that id.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "423": {
            "description": "The month is closed. The response carries the month in `details`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Remove a target",
        "tags": [
          "Plans"
        ]
      }
    },
    "/api/v1/expenses": {
      "get": {
        "description": "Returns expenses, newest month first, optionally filtered by month range, category, or the import that wrote them.\n\nThis is also the report's drill down. A report cell is one category in one month, which is `categoryId` with `from` and `to` set to the same month, so there is no separate drill down endpoint that could disagree with this one.\n\nFiltering by `importBatchId` answers the question a reviewer actually asks after an import, which is what that file put in.",
        "operationId": "ExpensesController_list_v1",
        "parameters": [
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "How many records to return.",
            "schema": {
              "minimum": 1,
              "maximum": 200,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "offset",
            "required": false,
            "in": "query",
            "description": "How many records to skip.",
            "schema": {
              "minimum": 0,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "from",
            "required": false,
            "in": "query",
            "description": "First month of the range, inclusive.",
            "schema": {
              "example": "2026-01",
              "type": "string"
            }
          },
          {
            "name": "to",
            "required": false,
            "in": "query",
            "description": "Last month of the range, inclusive.",
            "schema": {
              "example": "2026-12",
              "type": "string"
            }
          },
          {
            "name": "categoryId",
            "required": false,
            "in": "query",
            "description": "Restrict to one category.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0a1",
              "type": "string"
            }
          },
          {
            "name": "importBatchId",
            "required": false,
            "in": "query",
            "description": "Restrict to the expenses one import wrote.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0d1",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of expenses.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "items",
                    "pagination"
                  ],
                  "properties": {
                    "items": {
                      "type": "array",
                      "items": {
                        "$ref": "#/components/schemas/ExpenseResponseDTO"
                      }
                    },
                    "pagination": {
                      "$ref": "#/components/schemas/PaginationDTO"
                    }
                  }
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List expenses",
        "tags": [
          "Expenses"
        ]
      },
      "post": {
        "description": "Logs one expense against a category and a month.\n\nEach call creates a record. Logging twice against the same category and month is two expenses, not an overwrite, because that is what spending is: a month's total is the sum of its line items. The report adds them up. This is the opposite of a plan, where one category and month hold a single target.\n\nAn expense belongs to a month rather than a date. Nothing here is a timestamp, so no timezone can move spend into the wrong period.\n\nA negative amount is allowed and means money came back, such as a refund or a credit note.\n\nRejected with `423` when the month is closed, and with `404` when the category is not one the caller can use.",
        "operationId": "ExpensesController_create_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateExpenseDTO"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The stored expense.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExpenseResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The category is not one the caller can use.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "423": {
            "description": "The month is closed. The response carries the month in `details`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Log an expense",
        "tags": [
          "Expenses"
        ]
      }
    },
    "/api/v1/expenses/{expenseId}": {
      "patch": {
        "description": "Changes an expense's category, month, amount, or note. Omitted fields are left alone.\n\nThe month is changeable here, unlike on a plan. Spend genuinely lands in the wrong month, usually because an invoice was dated differently from when the cost was incurred, and correcting that is an edit rather than a different record.\n\nThat is why a month change is checked at both ends. Moving an expense out of a closed month changes that month's total, so it is an edit to a closed period just as much as moving one in. A request that touches a closed month at either end is rejected with `423` naming the month that blocked it.",
        "operationId": "ExpensesController_update_v1",
        "parameters": [
          {
            "name": "expenseId",
            "required": true,
            "in": "path",
            "description": "Identifier from the expense list.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0c1",
              "allOf": [
                {
                  "$ref": "#/components/schemas/ObjectId"
                }
              ]
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateExpenseDTO"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The updated expense.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExpenseResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The caller has no expense with that id, or the new category is not one they can use.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "423": {
            "description": "The month is closed. The response carries the month in `details`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Correct an expense",
        "tags": [
          "Expenses"
        ]
      },
      "delete": {
        "description": "Removes an expense, so it no longer counts toward the spend figure for its category and month.\n\nThe delete is soft. The audit trail keeps what the expense was, so a report run before the deletion can still be explained.\n\nRejected with `423` when the expense's month is closed.",
        "operationId": "ExpensesController_remove_v1",
        "parameters": [
          {
            "name": "expenseId",
            "required": true,
            "in": "path",
            "description": "Identifier from the expense list.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0c1",
              "allOf": [
                {
                  "$ref": "#/components/schemas/ObjectId"
                }
              ]
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The expense was removed."
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The caller has no expense with that id.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "423": {
            "description": "The month is closed. The response carries the month in `details`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Remove an expense",
        "tags": [
          "Expenses"
        ]
      }
    },
    "/api/v1/reports/plan-vs-spend": {
      "get": {
        "description": "One row per category and month in the range, with the target, what was spent, and the difference.\n\n**Variance** is `spend - plan`, so a negative number means under plan. **Variance percent** is `(spend - plan) / plan * 100` to two decimal places.\n\n**When the plan is 0, `variancePercent` is `null`.** Never `NaN`, never `Infinity`, and never a fabricated 100%. Dividing by zero has no answer, and a client renders `N/A`. The absolute `varianceMinor` is still correct and still worth showing: it is the whole of the unplanned spend.\n\n**A category with spend but no plan still appears.** That row is unplanned spend, which is usually the most interesting line in the report, and a naive join from the plan side would silently drop it.\n\n**Missing spend** follow `?missingSpend=`. Under `zero` (the default) a month with a target and nothing logged reports spend of 0 and a variance of the whole target. Under `null` the spend, variance, and percent are all `null` so a client can render a dash. Either way `hasSpend` says which happened, so a logged 0 is never confused with nothing logged.\n\n**Totals cover the whole range, not the page.** They are computed in the same database pass as the rows, so the summary cannot disagree with the table beneath it or shift when the reader turns a page.",
        "operationId": "ReportsController_planVsSpend_v1",
        "parameters": [
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "How many records to return.",
            "schema": {
              "minimum": 1,
              "maximum": 200,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "offset",
            "required": false,
            "in": "query",
            "description": "How many records to skip.",
            "schema": {
              "minimum": 0,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "from",
            "required": false,
            "in": "query",
            "description": "First month of the range, inclusive. Required unless `fiscalYear` is given.",
            "schema": {
              "example": "2026-01",
              "type": "string"
            }
          },
          {
            "name": "to",
            "required": false,
            "in": "query",
            "description": "Last month of the range, inclusive. Required unless `fiscalYear` is given.",
            "schema": {
              "example": "2026-02",
              "type": "string"
            }
          },
          {
            "name": "fiscalYear",
            "required": false,
            "in": "query",
            "description": "A fiscal year, resolved to its twelve months through this account’s `fiscalYearStartMonth`. With a start month of 4, `fiscalYear=2026` means 2026-04 through 2027-03. Takes precedence over `from` and `to`.",
            "schema": {
              "minimum": 1970,
              "maximum": 2999,
              "example": 2026,
              "type": "number"
            }
          },
          {
            "name": "categoryIds",
            "required": false,
            "in": "query",
            "description": "Restrict to these categories. Repeat the parameter, or send one comma separated value.",
            "schema": {
              "example": [
                "65f1c2d3e4b5a6c7d8e9f0a1"
              ],
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          {
            "name": "missingSpend",
            "required": false,
            "in": "query",
            "description": "How to report a category and month with a target but nothing logged. `zero` treats it as 0 spent, so the variance is the whole target. `null` reports the spend, variance, and percent as null, so a client can render a dash.",
            "schema": {
              "default": "zero",
              "type": "string",
              "enum": [
                "zero",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The report.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReportResponseDTO"
                }
              }
            }
          },
          "400": {
            "description": "The range ends before it starts, or a month is malformed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Plan against spend, with variance",
        "tags": [
          "Reports"
        ]
      }
    },
    "/api/v1/reports/plan-vs-spend/series": {
      "get": {
        "description": "Collapses the report onto one axis: `groupBy=month` gives a point per month summed across categories, `groupBy=category` gives a point per category summed across the range.\n\nUnpaginated, deliberately. A chart needs every point in the range, and one rebuilt from a page of table rows would draw a line that stops partway through the year.\n\nIt shares the aggregation with the table rather than reimplementing it, so a chart and the table beside it cannot disagree.",
        "operationId": "ReportsController_series_v1",
        "parameters": [
          {
            "name": "from",
            "required": false,
            "in": "query",
            "description": "First month of the range, inclusive. Required unless `fiscalYear` is given.",
            "schema": {
              "example": "2026-01",
              "type": "string"
            }
          },
          {
            "name": "to",
            "required": false,
            "in": "query",
            "description": "Last month of the range, inclusive. Required unless `fiscalYear` is given.",
            "schema": {
              "example": "2026-02",
              "type": "string"
            }
          },
          {
            "name": "fiscalYear",
            "required": false,
            "in": "query",
            "description": "A fiscal year, resolved to its twelve months through this account’s `fiscalYearStartMonth`. With a start month of 4, `fiscalYear=2026` means 2026-04 through 2027-03. Takes precedence over `from` and `to`.",
            "schema": {
              "minimum": 1970,
              "maximum": 2999,
              "example": 2026,
              "type": "number"
            }
          },
          {
            "name": "categoryIds",
            "required": false,
            "in": "query",
            "description": "Restrict to these categories. Repeat the parameter, or send one comma separated value.",
            "schema": {
              "example": [
                "65f1c2d3e4b5a6c7d8e9f0a1"
              ],
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          {
            "name": "groupBy",
            "required": false,
            "in": "query",
            "description": "Whether each point is a month or a category.",
            "schema": {
              "default": "month",
              "type": "string",
              "enum": [
                "month",
                "category"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The series.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SeriesResponseDTO"
                }
              }
            }
          },
          "400": {
            "description": "The range ends before it starts, or a month is malformed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "The same numbers, shaped for a chart",
        "tags": [
          "Reports"
        ]
      }
    },
    "/api/v1/reports/plan-vs-spend/export": {
      "get": {
        "description": "The same report as a file, with a labelled totals row appended.\n\nAmounts are written in major units the way a person writes them, `4800.00`, with no currency symbol and no thousands separator, so a spreadsheet reads the column as a number rather than as text.\n\nA variance percentage with no answer, which is what a plan of zero produces, is written as `-` rather than as `NaN` or left blank. A blank cell reads as missing data; a dash reads as deliberate.\n\nBuilt from the same call that serves the JSON report, so the file and the table on screen cannot disagree, and a download straight after viewing is served from the same cached result.",
        "operationId": "ReportsController_exportCsv_v1",
        "parameters": [
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "How many records to return.",
            "schema": {
              "minimum": 1,
              "maximum": 200,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "offset",
            "required": false,
            "in": "query",
            "description": "How many records to skip.",
            "schema": {
              "minimum": 0,
              "default": 0,
              "type": "number"
            }
          },
          {
            "name": "from",
            "required": false,
            "in": "query",
            "description": "First month of the range, inclusive. Required unless `fiscalYear` is given.",
            "schema": {
              "example": "2026-01",
              "type": "string"
            }
          },
          {
            "name": "to",
            "required": false,
            "in": "query",
            "description": "Last month of the range, inclusive. Required unless `fiscalYear` is given.",
            "schema": {
              "example": "2026-02",
              "type": "string"
            }
          },
          {
            "name": "fiscalYear",
            "required": false,
            "in": "query",
            "description": "A fiscal year, resolved to its twelve months through this account’s `fiscalYearStartMonth`. With a start month of 4, `fiscalYear=2026` means 2026-04 through 2027-03. Takes precedence over `from` and `to`.",
            "schema": {
              "minimum": 1970,
              "maximum": 2999,
              "example": 2026,
              "type": "number"
            }
          },
          {
            "name": "categoryIds",
            "required": false,
            "in": "query",
            "description": "Restrict to these categories. Repeat the parameter, or send one comma separated value.",
            "schema": {
              "example": [
                "65f1c2d3e4b5a6c7d8e9f0a1"
              ],
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          {
            "name": "missingSpend",
            "required": false,
            "in": "query",
            "description": "How to report a category and month with a target but nothing logged. `zero` treats it as 0 spent, so the variance is the whole target. `null` reports the spend, variance, and percent as null, so a client can render a dash.",
            "schema": {
              "default": "zero",
              "type": "string",
              "enum": [
                "zero",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The report as CSV.",
            "content": {
              "text/csv": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "400": {
            "description": "The range ends before it starts, or a month is malformed.",
            "content": {
              "text/csv": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Download the report as CSV",
        "tags": [
          "Reports"
        ]
      }
    },
    "/api/v1/imports/expenses": {
      "post": {
        "description": "Reads a CSV of expenses and writes them all, or writes none of them.\n\n**Columns.** `category`, `month`, and `amount` are required; `note` is optional. Header matching ignores case and surrounding spaces, and extra columns are ignored rather than rejected, because a file exported from an accounting system carries plenty this import has no use for.\n\n```csv\ncategory,month,amount,note\nMarketing,2026-01,4800.00,Q1 campaign\nPayroll,2026-01,20500.00\n```\n\n**Amounts** are written the way a person writes them, not in minor units. They are parsed digit by digit rather than through floating point, so `4800.10` stores exactly 480010 and never 480009.\n\n**Fail closed.** Every row is validated, every category resolved, and every month checked against the period locks before anything is written. If one row is wrong, nothing is written and the response lists every problem with its line number. This matters more than it sounds: an import that wrote the good rows and stopped could not simply be re-uploaded after a fix, because that would double the rows that did land.\n\n**Idempotent.** `Idempotency-Key` is required. Sending the same key again returns the original batch without importing anything, so a client that retries after a timeout does not double a month's spend.\n\n**Limits.** 10,000 rows and 5 MB. Past that the right answer is a background job rather than a longer request.",
        "operationId": "ImportsController_importExpenses_v1",
        "parameters": [
          {
            "name": "Idempotency-Key",
            "in": "header",
            "description": "A key the client generates for this upload, such as a UUID. Sending the same key again returns the original result instead of importing a second time.",
            "required": true,
            "schema": {
              "type": "string",
              "example": "5f2a4c6e-9b1d-4d3a-8f7c-2e1b0a9d8c7b"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "type": "object",
                "required": [
                  "file"
                ],
                "properties": {
                  "file": {
                    "type": "string",
                    "format": "binary",
                    "description": "A CSV file."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "The import committed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportBatchResponseDTO"
                }
              }
            }
          },
          "400": {
            "description": "The file is missing a required column, has no rows, is too large, or is not readable as CSV.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "422": {
            "description": "At least one row was rejected. Nothing was written. `details.errors` carries the line numbers.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "423": {
            "description": "The month is closed. The response carries the month in `details`.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Import expenses from a CSV",
        "tags": [
          "Imports"
        ]
      }
    },
    "/api/v1/imports": {
      "get": {
        "description": "Every import this account has attempted, newest first, successful or not.\n\nA failed import is kept deliberately. Without the record, a user whose upload was rejected has only the HTTP response, which they have already closed by the time they ask what went wrong.",
        "operationId": "ImportsController_list_v1",
        "parameters": [
          {
            "name": "limit",
            "required": false,
            "in": "query",
            "description": "How many records to return.",
            "schema": {
              "minimum": 1,
              "maximum": 200,
              "default": 50,
              "type": "number"
            }
          },
          {
            "name": "offset",
            "required": false,
            "in": "query",
            "description": "How many records to skip.",
            "schema": {
              "minimum": 0,
              "default": 0,
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of imports.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "items",
                    "pagination"
                  ],
                  "properties": {
                    "items": {
                      "type": "array",
                      "items": {
                        "$ref": "#/components/schemas/ImportBatchResponseDTO"
                      }
                    },
                    "pagination": {
                      "$ref": "#/components/schemas/PaginationDTO"
                    }
                  }
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List imports",
        "tags": [
          "Imports"
        ]
      }
    },
    "/api/v1/imports/{batchId}": {
      "get": {
        "description": "The outcome of a single import, including the per row errors when it failed.\n\nTo see what a successful import actually wrote, list expenses filtered by this batch: `GET /expenses?importBatchId=...`.",
        "operationId": "ImportsController_getById_v1",
        "parameters": [
          {
            "name": "batchId",
            "required": true,
            "in": "path",
            "description": "Identifier from the import list.",
            "schema": {
              "example": "65f1c2d3e4b5a6c7d8e9f0d1",
              "allOf": [
                {
                  "$ref": "#/components/schemas/ObjectId"
                }
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The import.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportBatchResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "404": {
            "description": "The caller has no import with that id.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Read one import",
        "tags": [
          "Imports"
        ]
      }
    },
    "/api/v1/reports/nl-query": {
      "post": {
        "description": "Turns a question such as \"how did marketing do in Q1 2026\" into a report filter, runs it, and returns both.\n\n**The model never writes a query.** It is given exactly one tool whose schema is a report filter: a month range, some category names, and a policy. It cannot express a collection, a field, an operator, or a database, it never sees a connection string, and the names it may choose from are this account's own categories. Whatever it returns is then validated by the same DTO a hand written report request goes through, and executed by the same report service.\n\nThat means this endpoint cannot reach anything `GET /reports/plan-vs-spend` could not, whatever the model returns. A model that answers with nonsense produces a validation error, not a query.\n\n**The filter comes back with the data.** A user has to be able to see that \"last quarter\" was read as the months they meant, and an answer with no visible interpretation is one the reader has to trust blindly.\n\nReturns `503` when the deployment has no `ANTHROPIC_API_KEY`, and `502` when the model is unreachable or does not produce a usable filter. Every other endpoint works either way, so the report is always available directly.",
        "operationId": "NlQueryController_ask_v1",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NlQueryDTO"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The filter and the report it produced.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NlQueryResponseDTO"
                }
              }
            }
          },
          "401": {
            "description": "The access token is missing, expired, or invalid.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "502": {
            "description": "The model was unreachable or did not return a usable filter.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          },
          "503": {
            "description": "This deployment has no API key configured, so the endpoint is switched off.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponseDTO"
                }
              }
            }
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Ask about spending in plain language",
        "tags": [
          "Reports"
        ]
      }
    },
    "/api/v1/health": {
      "get": {
        "description": "Answers whether the process is running. Checks no dependency.",
        "operationId": "HealthController_liveness_v1",
        "parameters": [],
        "responses": {
          "200": {
            "description": "The Health Check is successful",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": {
                      "type": "string",
                      "example": "ok"
                    },
                    "info": {
                      "type": "object",
                      "example": {
                        "database": {
                          "status": "up"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      },
                      "nullable": true
                    },
                    "error": {
                      "type": "object",
                      "example": {},
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      },
                      "nullable": true
                    },
                    "details": {
                      "type": "object",
                      "example": {
                        "database": {
                          "status": "up"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      }
                    }
                  }
                }
              }
            }
          },
          "503": {
            "description": "The Health Check is not successful",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": {
                      "type": "string",
                      "example": "error"
                    },
                    "info": {
                      "type": "object",
                      "example": {
                        "database": {
                          "status": "up"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      },
                      "nullable": true
                    },
                    "error": {
                      "type": "object",
                      "example": {
                        "redis": {
                          "status": "down",
                          "message": "Could not connect"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      },
                      "nullable": true
                    },
                    "details": {
                      "type": "object",
                      "example": {
                        "database": {
                          "status": "up"
                        },
                        "redis": {
                          "status": "down",
                          "message": "Could not connect"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "summary": "Liveness probe",
        "tags": [
          "Health"
        ]
      }
    },
    "/api/v1/health/ready": {
      "get": {
        "description": "Answers whether this instance can serve requests.",
        "operationId": "HealthController_readiness_v1",
        "parameters": [],
        "responses": {
          "200": {
            "description": "The Health Check is successful",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": {
                      "type": "string",
                      "example": "ok"
                    },
                    "info": {
                      "type": "object",
                      "example": {
                        "database": {
                          "status": "up"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      },
                      "nullable": true
                    },
                    "error": {
                      "type": "object",
                      "example": {},
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      },
                      "nullable": true
                    },
                    "details": {
                      "type": "object",
                      "example": {
                        "database": {
                          "status": "up"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      }
                    }
                  }
                }
              }
            }
          },
          "503": {
            "description": "The Health Check is not successful",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": {
                      "type": "string",
                      "example": "error"
                    },
                    "info": {
                      "type": "object",
                      "example": {
                        "database": {
                          "status": "up"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      },
                      "nullable": true
                    },
                    "error": {
                      "type": "object",
                      "example": {
                        "redis": {
                          "status": "down",
                          "message": "Could not connect"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      },
                      "nullable": true
                    },
                    "details": {
                      "type": "object",
                      "example": {
                        "database": {
                          "status": "up"
                        },
                        "redis": {
                          "status": "down",
                          "message": "Could not connect"
                        }
                      },
                      "additionalProperties": {
                        "type": "object",
                        "required": [
                          "status"
                        ],
                        "properties": {
                          "status": {
                            "type": "string"
                          }
                        },
                        "additionalProperties": true
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "summary": "Readiness probe",
        "tags": [
          "Health"
        ]
      }
    }
  },
  "info": {
    "title": "Kasse API",
    "description": "Monthly spending targets, logged expenses, and variance reporting with locked periods.\n\n**Money** is an integer count of minor units. A field ending in `Minor` holds cents.\n\n**Months** are the string `YYYY-MM`.\n\n**Variance** is `spend - plan`. Variance percent is `null` when the plan is zero, never `NaN`.\n\n**Missing spend** default to `0`. Pass `missingSpend=null` to receive `null` instead. Every report row carries `hasSpend`, so a logged zero is never confused with nothing logged.\n\n**Locked periods** reject writes with `423` and the code `PERIOD_LOCKED`.\n\n**Deletes are soft.** A `DELETE` answers `204` and the record stops appearing in reads.\n\n**Sessions** travel in the `Authorization` header, never a cookie, so a mobile or desktop client is a first class caller. Access tokens are RS256 signed and short lived; refresh tokens are opaque, single use, and rotate.",
    "version": "1.0.0",
    "contact": {}
  },
  "tags": [
    {
      "name": "Auth",
      "description": "Establishing, renewing, and ending sessions."
    },
    {
      "name": "Account",
      "description": "The signed in account and the settings reports are computed against."
    },
    {
      "name": "Categories",
      "description": "The shared catalogue and the account’s own categories."
    },
    {
      "name": "Period locks",
      "description": "Closing and reopening accounting periods. A closed period is read only."
    },
    {
      "name": "Plans",
      "description": "Monthly spending targets, the plan side of the report."
    },
    {
      "name": "Audit log",
      "description": "The append only trail of changes to financial data."
    },
    {
      "name": "Health",
      "description": "Liveness and readiness probes."
    }
  ],
  "servers": [],
  "components": {
    "securitySchemes": {
      "bearer": {
        "scheme": "bearer",
        "bearerFormat": "JWT",
        "type": "http"
      }
    },
    "schemas": {
      "ErrorResponseDTO": {
        "type": "object",
        "properties": {
          "statusCode": {
            "type": "number",
            "description": "HTTP status, repeated in the body.",
            "example": 423
          },
          "code": {
            "type": "string",
            "description": "Stable code a client branches on. Never parse the message.",
            "enum": [
              "VALIDATION_FAILED",
              "UNAUTHENTICATED",
              "FORBIDDEN",
              "NOT_FOUND",
              "CONFLICT",
              "IMPORT_VALIDATION_FAILED",
              "PERIOD_LOCKED",
              "RATE_LIMITED",
              "INTERNAL"
            ],
            "example": "PERIOD_LOCKED"
          },
          "message": {
            "type": "string",
            "description": "What went wrong, written for a person.",
            "example": "2026-01 is locked and cannot be edited."
          },
          "details": {
            "type": "object",
            "description": "Machine readable specifics: the offending month, the failing rows, the invalid fields.",
            "example": {
              "month": "2026-01"
            }
          },
          "requestId": {
            "type": "string",
            "description": "Correlates this response with its log lines and the x-request-id header.",
            "example": "0f9c1e2a-7b3d-4c5e-9a8f-1b2c3d4e5f6a"
          },
          "path": {
            "type": "string",
            "description": "The path that produced the error.",
            "example": "/api/v1/plans"
          },
          "timestamp": {
            "type": "string",
            "description": "When the error was produced.",
            "example": "2026-01-15T10:04:11.212Z",
            "format": "date-time"
          }
        },
        "required": [
          "statusCode",
          "code",
          "message",
          "requestId",
          "path",
          "timestamp"
        ]
      },
      "CredentialsDTO": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "description": "Login address.",
            "example": "finance@acme.test",
            "format": "email",
            "maxLength": 254
          },
          "password": {
            "type": "string",
            "description": "Password. At least 12 characters. No composition rules: length is what resists an offline attack.",
            "example": "correct horse battery staple",
            "minLength": 12,
            "maxLength": 128,
            "format": "password"
          }
        },
        "required": [
          "email",
          "password"
        ]
      },
      "UserResponseDTO": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier of the account.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "email": {
            "type": "string",
            "description": "Login address.",
            "example": "finance@acme.test",
            "format": "email"
          },
          "currency": {
            "type": "string",
            "description": "Currency every amount on this account is denominated in.",
            "enum": [
              "USD",
              "AED"
            ],
            "example": "USD"
          },
          "fiscalYearStartMonth": {
            "type": "number",
            "description": "Month the fiscal year starts. January means the fiscal year is the calendar year.",
            "example": 1,
            "minimum": 1,
            "maximum": 12
          },
          "createdAt": {
            "type": "string",
            "description": "When the account was created.",
            "example": "2026-01-15T10:04:11.212Z",
            "format": "date-time"
          }
        },
        "required": [
          "id",
          "email",
          "currency",
          "fiscalYearStartMonth",
          "createdAt"
        ]
      },
      "AuthResponseDTO": {
        "type": "object",
        "properties": {
          "accessToken": {
            "type": "string",
            "description": "Short lived access token. Send as `Authorization: Bearer <token>`.",
            "example": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9..."
          },
          "expiresIn": {
            "type": "number",
            "description": "Seconds until the access token expires.",
            "example": 900
          },
          "refreshToken": {
            "type": "string",
            "description": "Long lived refresh token. Single use: exchanging it returns a new pair and invalidates this one. Presenting it twice revokes every session descended from this login.",
            "example": "V2hhdCBhIGxvdmVseSBkYXkgZm9yIGEgcmVmcmVzaCB0b2tlbg"
          },
          "user": {
            "description": "The account this session belongs to.",
            "allOf": [
              {
                "$ref": "#/components/schemas/UserResponseDTO"
              }
            ]
          }
        },
        "required": [
          "accessToken",
          "expiresIn",
          "refreshToken",
          "user"
        ]
      },
      "RefreshTokenDTO": {
        "type": "object",
        "properties": {
          "refreshToken": {
            "type": "string",
            "description": "The refresh token issued by signup, login, or a previous refresh.",
            "example": "V2hhdCBhIGxvdmVseSBkYXkgZm9yIGEgcmVmcmVzaCB0b2tlbg",
            "minLength": 20,
            "maxLength": 200
          }
        },
        "required": [
          "refreshToken"
        ]
      },
      "SessionResponseDTO": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier, used to revoke this session.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "createdAt": {
            "type": "string",
            "description": "When the session was established.",
            "example": "2026-01-15T10:04:11.212Z",
            "format": "date-time"
          },
          "lastUsedAt": {
            "type": "object",
            "description": "When this session last refreshed. Null if it never has.",
            "example": "2026-01-16T08:12:00.000Z",
            "format": "date-time",
            "nullable": true
          },
          "expiresAt": {
            "type": "string",
            "description": "When the session expires.",
            "example": "2026-01-22T10:04:11.212Z",
            "format": "date-time"
          }
        },
        "required": [
          "id",
          "createdAt",
          "lastUsedAt",
          "expiresAt"
        ]
      },
      "ObjectId": {
        "type": "object",
        "properties": {}
      },
      "UpdateMeDTO": {
        "type": "object",
        "properties": {
          "currency": {
            "type": "string",
            "description": "Currency the account is denominated in. Changing it relabels existing amounts, it does not convert them.",
            "enum": [
              "USD",
              "AED"
            ],
            "example": "AED"
          },
          "fiscalYearStartMonth": {
            "type": "number",
            "description": "Month the fiscal year starts. 1 is January, which makes the fiscal year the calendar year.",
            "example": 4,
            "minimum": 1,
            "maximum": 12
          }
        }
      },
      "AuditEntryResponseDTO": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier of the entry.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "action": {
            "type": "string",
            "description": "What happened.",
            "enum": [
              "CATEGORY_CREATED",
              "CATEGORY_UPDATED",
              "CATEGORY_DELETED",
              "PLAN_CREATED",
              "PLAN_UPDATED",
              "PLAN_DELETED",
              "EXPENSE_CREATED",
              "EXPENSE_UPDATED",
              "EXPENSE_DELETED",
              "PERIOD_LOCKED",
              "PERIOD_UNLOCKED",
              "IMPORT_COMPLETED",
              "NL_QUERY_RUN"
            ],
            "example": "PLAN_UPDATED"
          },
          "entity": {
            "type": "string",
            "description": "What kind of record it happened to.",
            "enum": [
              "CATEGORY",
              "PLAN",
              "EXPENSE",
              "PERIOD_LOCK",
              "IMPORT_BATCH",
              "REPORT"
            ],
            "example": "PLAN"
          },
          "entityId": {
            "type": "object",
            "description": "Which record.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a2",
            "nullable": true
          },
          "before": {
            "type": "object",
            "description": "State before the change. Null on a creation.",
            "example": {
              "targetMinor": 500000
            },
            "nullable": true
          },
          "after": {
            "type": "object",
            "description": "State after the change. Null on a deletion.",
            "example": {
              "targetMinor": 600000
            },
            "nullable": true
          },
          "requestId": {
            "type": "object",
            "description": "The request that made the change. Matches the x-request-id header and the log lines.",
            "example": "0f9c1e2a-7b3d-4c5e-9a8f-1b2c3d4e5f6a",
            "nullable": true
          },
          "at": {
            "type": "string",
            "description": "When it happened.",
            "example": "2026-01-15T10:04:11.212Z",
            "format": "date-time"
          }
        },
        "required": [
          "id",
          "action",
          "entity",
          "at"
        ]
      },
      "PaginationDTO": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "number",
            "description": "How many records were requested.",
            "example": 50
          },
          "offset": {
            "type": "number",
            "description": "How many records were skipped.",
            "example": 0
          },
          "total": {
            "type": "number",
            "description": "How many records match the filter, across every page.",
            "example": 4
          }
        },
        "required": [
          "limit",
          "offset",
          "total"
        ]
      },
      "CategoryResponseDTO": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier, used when creating a plan or an expense.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "name": {
            "type": "string",
            "description": "The name as written.",
            "example": "Cloud Hosting"
          },
          "shared": {
            "type": "boolean",
            "description": "Whether this comes from the shared catalogue. Shared categories are readable by every account and editable by none.",
            "example": false
          },
          "archived": {
            "type": "boolean",
            "description": "Whether it is hidden from pickers. An archived category still resolves in existing plans and reports.",
            "example": false
          }
        },
        "required": [
          "id",
          "name",
          "shared",
          "archived"
        ]
      },
      "CreateCategoryDTO": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Name as it should be displayed. Uniqueness is checked case and spacing insensitively, so \"Cloud Hosting\" and \"cloud  hosting\" are the same category.",
            "example": "Cloud Hosting",
            "minLength": 1,
            "maxLength": 60
          }
        },
        "required": [
          "name"
        ]
      },
      "UpdateCategoryDTO": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "A new display name.",
            "example": "Cloud Infrastructure",
            "minLength": 1,
            "maxLength": 60
          },
          "archived": {
            "type": "boolean",
            "description": "Hide the category from pickers without removing it. Existing plans and expenses keep resolving it, and reports are unaffected.",
            "example": true
          }
        }
      },
      "PeriodLockResponseDTO": {
        "type": "object",
        "properties": {
          "month": {
            "type": "string",
            "description": "The closed month.",
            "example": "2026-01"
          },
          "lockedAt": {
            "type": "string",
            "description": "When it was closed.",
            "example": "2026-02-01T09:00:00.000Z",
            "format": "date-time"
          }
        },
        "required": [
          "month",
          "lockedAt"
        ]
      },
      "CreatePeriodLockDTO": {
        "type": "object",
        "properties": {
          "months": {
            "description": "Months to close, each as YYYY-MM. Locking a month that is already closed is harmless.",
            "example": [
              "2026-01",
              "2026-02"
            ],
            "maxItems": 15,
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "quarter": {
            "type": "string",
            "description": "A calendar quarter to close, which expands to its three months. Q1 is January through March.",
            "example": "2026-Q1"
          }
        }
      },
      "PlanResponseDTO": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier of the target.",
            "example": "65f1c2d3e4b5a6c7d8e9f0b1"
          },
          "categoryId": {
            "type": "string",
            "description": "The category this target is for.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "month": {
            "type": "string",
            "description": "The month it applies to.",
            "example": "2026-01"
          },
          "targetMinor": {
            "type": "number",
            "description": "The target in minor units. 5,000.00 is 500000.",
            "example": 500000
          }
        },
        "required": [
          "id",
          "categoryId",
          "month",
          "targetMinor"
        ]
      },
      "UpsertPlanDTO": {
        "type": "object",
        "properties": {
          "categoryId": {
            "type": "string",
            "description": "The category this target is for.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "month": {
            "type": "string",
            "description": "The month the target applies to.",
            "example": "2026-01",
            "pattern": "^\\d{4}-(0[1-9]|1[0-2])$"
          },
          "targetMinor": {
            "type": "number",
            "description": "The target in minor units. 5,000.00 is 500000. Zero is a meaningful target: it means nothing was planned, and any spend against it is reported as unplanned rather than as a percentage.",
            "example": 500000,
            "minimum": 0
          }
        },
        "required": [
          "categoryId",
          "month",
          "targetMinor"
        ]
      },
      "UpdatePlanDTO": {
        "type": "object",
        "properties": {
          "targetMinor": {
            "type": "number",
            "description": "The new target in minor units. 5,000.00 is 500000.",
            "example": 600000,
            "minimum": 0
          }
        },
        "required": [
          "targetMinor"
        ]
      },
      "ExpenseResponseDTO": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier of the expense.",
            "example": "65f1c2d3e4b5a6c7d8e9f0c1"
          },
          "categoryId": {
            "type": "string",
            "description": "The category it belongs to.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "month": {
            "type": "string",
            "description": "The month it belongs to.",
            "example": "2026-01"
          },
          "amountMinor": {
            "type": "number",
            "description": "The amount in minor units. Negative means money came back.",
            "example": 480000
          },
          "note": {
            "type": "object",
            "description": "Free text.",
            "example": "Annual renewal",
            "nullable": true
          },
          "source": {
            "type": "string",
            "description": "How it got here.",
            "enum": [
              "MANUAL",
              "CSV"
            ],
            "example": "MANUAL"
          },
          "importBatchId": {
            "type": "object",
            "description": "The import that wrote it, when it came from a file.",
            "example": "65f1c2d3e4b5a6c7d8e9f0d1",
            "nullable": true
          },
          "createdAt": {
            "type": "string",
            "description": "When it was logged.",
            "example": "2026-01-15T10:04:11.212Z",
            "format": "date-time"
          }
        },
        "required": [
          "id",
          "categoryId",
          "month",
          "amountMinor",
          "source",
          "createdAt"
        ]
      },
      "CreateExpenseDTO": {
        "type": "object",
        "properties": {
          "categoryId": {
            "type": "string",
            "description": "The category the expense belongs to.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "month": {
            "type": "string",
            "description": "The month the expense belongs to. An expense belongs to a month, not to a date, so nothing here is a timestamp.",
            "example": "2026-01"
          },
          "amountMinor": {
            "type": "number",
            "description": "The amount in minor units. 4,800.00 is 480000. Negative is allowed and means money came back, such as a refund or a credit note.",
            "example": 480000
          },
          "note": {
            "type": "string",
            "description": "Optional free text.",
            "example": "Annual renewal",
            "maxLength": 280
          }
        },
        "required": [
          "categoryId",
          "month",
          "amountMinor"
        ]
      },
      "UpdateExpenseDTO": {
        "type": "object",
        "properties": {
          "categoryId": {
            "type": "string",
            "description": "Move the expense to a different category.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "month": {
            "type": "string",
            "description": "Move the expense to a different month. Both the old and the new month must be open.",
            "example": "2026-02"
          },
          "amountMinor": {
            "type": "number",
            "description": "Correct the amount, in minor units.",
            "example": 490000
          },
          "note": {
            "type": "string",
            "description": "Replace the note.",
            "example": "Corrected after credit note",
            "maxLength": 280
          }
        }
      },
      "ReportRowDTO": {
        "type": "object",
        "properties": {
          "categoryId": {
            "type": "string",
            "description": "The category.",
            "example": "65f1c2d3e4b5a6c7d8e9f0a1"
          },
          "categoryName": {
            "type": "string",
            "description": "The category name.",
            "example": "Marketing"
          },
          "month": {
            "type": "string",
            "description": "The month.",
            "example": "2026-01"
          },
          "planMinor": {
            "type": "number",
            "description": "The target, in minor units.",
            "example": 500000
          },
          "spentMinor": {
            "type": "object",
            "description": "Logged spend, in minor units. Null only when nothing was logged and the policy is `null`.",
            "example": 480000,
            "nullable": true
          },
          "varianceMinor": {
            "type": "object",
            "description": "Spend minus plan. Negative means under plan.",
            "example": -20000,
            "nullable": true
          },
          "variancePercent": {
            "type": "object",
            "description": "The variance as a percentage of the plan, to two decimal places. **Null when the plan is 0**, because dividing by zero has no answer. Never `NaN` and never `Infinity`.",
            "example": -4,
            "nullable": true
          },
          "hasPlan": {
            "type": "boolean",
            "description": "Whether a target exists. A target of 0 is not the same as no target.",
            "example": true
          },
          "hasSpend": {
            "type": "boolean",
            "description": "Whether anything was logged. Logged 0 is not the same as nothing logged.",
            "example": true
          }
        },
        "required": [
          "categoryId",
          "categoryName",
          "month",
          "planMinor",
          "hasPlan",
          "hasSpend"
        ]
      },
      "ReportTotalsDTO": {
        "type": "object",
        "properties": {
          "planMinor": {
            "type": "number",
            "description": "Every target in range, in minor units.",
            "example": 2500000
          },
          "spentMinor": {
            "type": "number",
            "description": "Every expense in range, in minor units.",
            "example": 2530000
          },
          "varianceMinor": {
            "type": "number",
            "description": "Spend minus plan across the range.",
            "example": 30000
          },
          "variancePercent": {
            "type": "object",
            "description": "The variance as a percentage of the total plan. Null when the total plan is 0.",
            "example": 1.2,
            "nullable": true
          }
        },
        "required": [
          "planMinor",
          "spentMinor",
          "varianceMinor"
        ]
      },
      "ReportResponseDTO": {
        "type": "object",
        "properties": {
          "items": {
            "description": "The rows on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ReportRowDTO"
            }
          },
          "totals": {
            "description": "Totals across the whole range, not just this page.",
            "allOf": [
              {
                "$ref": "#/components/schemas/ReportTotalsDTO"
              }
            ]
          },
          "pagination": {
            "description": "Where this page sits in the whole result set.",
            "allOf": [
              {
                "$ref": "#/components/schemas/PaginationDTO"
              }
            ]
          }
        },
        "required": [
          "items",
          "totals",
          "pagination"
        ]
      },
      "SeriesPointDTO": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The month, or the category id, this point is grouped under.",
            "example": "2026-01"
          },
          "label": {
            "type": "string",
            "description": "What to render on the axis.",
            "example": "2026-01"
          },
          "planMinor": {
            "type": "number",
            "description": "Planned, summed for this point.",
            "example": 2500000
          },
          "spentMinor": {
            "type": "number",
            "description": "Logged, summed for this point.",
            "example": 2530000
          },
          "varianceMinor": {
            "type": "number",
            "description": "Spend minus plan for this point.",
            "example": 30000
          }
        },
        "required": [
          "key",
          "label",
          "planMinor",
          "spentMinor",
          "varianceMinor"
        ]
      },
      "SeriesResponseDTO": {
        "type": "object",
        "properties": {
          "groupBy": {
            "type": "string",
            "description": "Which axis the points are grouped on.",
            "enum": [
              "month",
              "category"
            ]
          },
          "points": {
            "description": "The points, in axis order.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SeriesPointDTO"
            }
          }
        },
        "required": [
          "groupBy",
          "points"
        ]
      },
      "ImportRowErrorDTO": {
        "type": "object",
        "properties": {
          "line": {
            "type": "number",
            "description": "The line in the uploaded file. The header is line 1.",
            "example": 7
          },
          "column": {
            "type": "object",
            "description": "Which column was wrong.",
            "example": "month",
            "nullable": true
          },
          "message": {
            "type": "string",
            "description": "What was wrong.",
            "example": "\"2026-13\" is not a month in YYYY-MM format."
          }
        },
        "required": [
          "line",
          "column",
          "message"
        ]
      },
      "ImportBatchResponseDTO": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identifier of the batch.",
            "example": "65f1c2d3e4b5a6c7d8e9f0d1"
          },
          "filename": {
            "type": "string",
            "description": "The file that was uploaded.",
            "example": "january-expenses.csv"
          },
          "status": {
            "type": "string",
            "description": "Whether the rows were written.",
            "enum": [
              "COMPLETED",
              "FAILED"
            ]
          },
          "rowCount": {
            "type": "number",
            "description": "How many data rows the file carried.",
            "example": 120
          },
          "errorCount": {
            "type": "number",
            "description": "How many rows were rejected.",
            "example": 0
          },
          "expenseCount": {
            "type": "number",
            "description": "How many expenses this batch wrote. Zero when it failed.",
            "example": 120
          },
          "errors": {
            "description": "The rejected rows, in file order.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ImportRowErrorDTO"
            }
          },
          "createdAt": {
            "type": "string",
            "description": "When it was uploaded.",
            "example": "2026-01-15T10:04:11.212Z",
            "format": "date-time"
          }
        },
        "required": [
          "id",
          "filename",
          "status",
          "rowCount",
          "errorCount",
          "expenseCount",
          "errors",
          "createdAt"
        ]
      },
      "NlQueryDTO": {
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "description": "A question about spending, in plain language.",
            "example": "How did marketing do in Q1 2026?",
            "maxLength": 500
          }
        },
        "required": [
          "question"
        ]
      },
      "NlQueryFilterDTO": {
        "type": "object",
        "properties": {
          "interpretation": {
            "type": "string",
            "description": "One sentence restating the question as it was understood.",
            "example": "Marketing spend against plan for January to March 2026."
          },
          "from": {
            "type": "string",
            "description": "First month the report covers.",
            "example": "2026-01"
          },
          "to": {
            "type": "string",
            "description": "Last month the report covers.",
            "example": "2026-03"
          },
          "categories": {
            "description": "Categories it was narrowed to. Empty means every category.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "missingSpend": {
            "type": "string",
            "description": "The policy applied to months with nothing logged.",
            "enum": [
              "zero",
              "null"
            ]
          }
        },
        "required": [
          "interpretation",
          "from",
          "to",
          "categories"
        ]
      },
      "NlQueryResponseDTO": {
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "description": "The question as asked.",
            "example": "How did marketing do in Q1 2026?"
          },
          "filter": {
            "description": "How the question was read.",
            "allOf": [
              {
                "$ref": "#/components/schemas/NlQueryFilterDTO"
              }
            ]
          },
          "report": {
            "description": "The report that filter produced.",
            "allOf": [
              {
                "$ref": "#/components/schemas/ReportResponseDTO"
              }
            ]
          }
        },
        "required": [
          "question",
          "filter",
          "report"
        ]
      }
    }
  }
}
